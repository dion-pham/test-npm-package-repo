# @dionpham/my-scoped-package

A lightweight logger for Node.js that prints color-coded messages to the terminal. Great for CLI tools and debugging.

## Installation

```bash
npm install @dionpham/my-scoped-package
